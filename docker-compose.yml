version: "3.9"

services:
  finpilot:
    build: .
    env_file: .env
    volumes:
      - ./uploads:/app/uploads
      - ./finpilot.db:/app/finpilot.db
    restart: unless-stopped

  # Uncomment for PostgreSQL in production
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: finpilot
  #     POSTGRES_PASSWORD: finpilot_secret
  #     POSTGRES_DB: finpilot
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   restart: unless-stopped

# volumes:
#   pgdata:
